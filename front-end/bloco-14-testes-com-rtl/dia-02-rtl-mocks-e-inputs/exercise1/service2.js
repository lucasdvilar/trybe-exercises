const turnUppercase = (string) => string.toUpperCase();

const getFirstLetter = (string) => string[0];

const concatTwoStrings = (string1, string2) => string1 + string2;

module.exports = {
  turnUppercase,
  getFirstLetter,
  concatTwoStrings,
};